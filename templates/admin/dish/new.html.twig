{% extends 'admin/base.html.twig' %}

{% block title %}Nouveau plat - The Klassiker Admin{% endblock %}
{% block page_title %}Nouveau plat{% endblock %}

{% block body %}
<div class="table-card">
  <h2>Créer un nouveau plat</h2>
  
  {{ form_start(form) }}
    {{ form_widget(form) }}
  {{ form_end(form) }}
  
  <div class="mt-3">
    <a href="{{ path('admin_dish_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
  </div>
</div>
{% endblock %}

