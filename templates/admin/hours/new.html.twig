{% extends 'admin/base.html.twig' %}

{% block title %}Nouvel horaire - The Klassiker Admin{% endblock %}
{% block page_title %}Nouvel horaire{% endblock %}

{% block body %}
<div class="table-card">
  <h2>Créer un nouvel horaire</h2>
  
  {{ form_start(form) }}
    {{ form_widget(form) }}
  {{ form_end(form) }}
  
  <div class="mt-3">
    <a href="{{ path('admin_hours_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
  </div>
</div>
{% endblock %}

